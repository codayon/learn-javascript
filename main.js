// dumb project with js (learning)

// arrays

let x = ['x', 'y', 'z'];

console.log(x[0]);

// objects

let y = {
  x: 'x',
  y: 'y',
  z: 'z',
};

console.log(y.y);

// defined objects
let z = {
  x: 'x',
  y: 'y',
  z: 'z',
};

console.log(z.z);

console.log('==========');

// for loop

let number = 7;

for (i = 1; i <= 10; ++i) {
  console.log(number + ' x ' + i + ' = ' + number * i);
}
